[package]
name = "fastq-clean-local"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
name = "fqclean"
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = true
panic = "abort"
codegen-units = 1
